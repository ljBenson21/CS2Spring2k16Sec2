#include <iostream>
#include <string>
#include "Event.h"
#include "Venue.h"
using namespace std;

bool Venue :: validTime(int time)
{
	for (int i = 0; i < 12; i++)
	{
		scheduleEvents[i].getTime;
		if (time == )
	}
}